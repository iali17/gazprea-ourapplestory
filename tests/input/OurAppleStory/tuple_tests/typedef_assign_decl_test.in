typedef tuple(integer t, integer d, integer f) doubleT;
 
procedure main() returns integer { 
  doubleT x = (12,3,1);
  var out = std_output();

  x.t -> out; '\n' -> out;
  x.2 -> out; '\n' -> out;

  x = (1, 3, 12);

  x.t -> out; '\n' -> out;
  x.2 -> out; '\n' -> out;

  return 0; 
}
